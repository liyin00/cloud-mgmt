apiVersion: networking.k8s.io/v1

kind: Ingress

metadata:
  name: user-app-ingress

spec:
  rules: 
    - http: 
        paths:
          - path: /
            pathType: Prefix
            backend: 
              Service: 
                name: users
                port: 
                  number: 5003 